@import "tailwindcss";

/*
 * Tailwind migration (A)
 * - Existing class names are intentionally preserved.
 * - Tailwind v4 is loaded first.
 * - The existing selectors remain as component-level CSS so the current UI
 *   keeps working without changing HTML/JSX.
 * - Theme colors are exposed to Tailwind utilities via @theme.
 */

@theme {
  --color-maroon: #7a1f2b;
  --color-maroon-dark: #5e1620;
  --color-green: #2e7d5b;
  --color-green-light: #e8f5ee;
  --color-teal: #2497a3;
  --color-ink: #22262b;
  --color-muted: #6b7280;
  --color-line: #e6e7eb;
  --color-page-bg: #f4f5f7;
  --color-panel: #ffffff;
  --color-red: #c0392b;
  --color-amber: #c98a1f;
}

@layer base {
  *, ::before, ::after { box-sizing: border-box; }
  body {
    margin: 0;
    font-family: "Segoe UI", "Noto Sans Thai", Arial, sans-serif;
    background: var(--color-page-bg);
    color: var(--color-ink);
    font-size: 13px;
  }
  button { font-family: inherit; }
}

.state-msg{padding:40px;text-align:center;color:var(--color-muted);font-size:14px;}

.app{display:flex;min-height:100vh;}

/* Sidebar */
.sidebar{width:210px;background:var(--color-panel);border-right:1px solid var(--color-line);display:flex;flex-direction:column;flex-shrink:0;}
.brand{display:flex;align-items:center;gap:8px;padding:16px 18px;border-bottom:1px solid var(--color-line);}
.brand-mark{width:30px;height:30px;border-radius:6px;background:var(--color-maroon);color:#fff;display:flex;align-items:center;justify-content:center;font-weight:700;}
.brand-name{font-weight:700;font-size:15px;color:var(--color-maroon-dark);}
.brand-sub{font-size:10px;color:var(--color-muted);}
.side-nav{padding:12px 10px;flex:1;}
.nav-item{padding:10px 12px;border-radius:8px;color:#4b5563;cursor:pointer;margin-bottom:2px;}
.nav-item:hover{background:#f4f5f7;}
.nav-item.active{background:var(--color-green-light);color:var(--color-green);font-weight:600;}
.side-settings{padding:12px 10px;border-top:1px solid var(--color-line);}
.new-task-btn{margin:12px;padding:11px;border-radius:8px;background:var(--color-green);color:#fff;border:none;font-weight:600;cursor:pointer;}
.sidebar-backdrop{display:none;}

/* Main */
.main{flex:1;display:flex;flex-direction:column;min-width:0;}
.topbar{background:var(--color-panel);border-bottom:1px solid var(--color-line);display:flex;align-items:center;justify-content:space-between;padding:10px 22px;}
.topbar-left{display:flex;align-items:center;gap:10px;}
.app-name{font-weight:700;font-size:16px;color:var(--color-maroon-dark);}
.pill{background:var(--color-teal);color:#fff;font-size:11px;font-weight:600;padding:3px 10px;border-radius:10px;}
.hamburger{display:none;background:none;border:none;font-size:18px;cursor:pointer;color:var(--color-maroon-dark);}

.content{flex:1;padding:18px 22px;}

.case-header{display:flex;align-items:flex-start;justify-content:space-between;gap:10px;flex-wrap:wrap;margin-bottom:4px;}
.case-title{font-size:17px;font-weight:700;color:var(--color-maroon-dark);}
.case-meta{font-size:12px;color:var(--color-muted);margin-top:6px;display:flex;align-items:center;gap:10px;flex-wrap:wrap;}
.case-badges{display:flex;gap:8px;flex-wrap:wrap;align-items:center;}
.badge-warn{background:#fdeceb;color:var(--color-red);font-size:11px;font-weight:700;padding:5px 12px;border-radius:14px;}
.badge-stage{background:#eef1f4;color:#374151;font-size:11px;font-weight:600;padding:4px 10px;border-radius:12px;}
.badge-acctmark-outline{background:#fff;border:1px solid var(--color-green);color:var(--color-green);font-size:11px;font-weight:600;padding:4px 10px;border-radius:6px;}
.badge-acctmark-solid{background:var(--color-green-light);color:var(--color-green);font-size:12px;font-weight:700;padding:6px 12px;border-radius:6px;}

.grid{display:grid;grid-template-columns:1fr 300px;gap:18px;margin-top:14px;align-items:start;}

.panel{background:var(--color-panel);border:1px solid var(--color-line);border-radius:10px;padding:16px 18px;margin-bottom:16px;}
.panel h4{margin:0 0 12px;font-size:12px;color:var(--color-green);font-weight:700;border-bottom:2px solid var(--color-green);display:inline-block;padding-bottom:4px;}

.info-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:12px 20px;margin-bottom:14px;}
.field-label{font-size:11px;color:var(--color-muted);margin-bottom:2px;}
.field-value{font-size:13px;font-weight:600;word-break:break-word;}

.contact-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;background:#fafbfc;border:1px solid var(--color-line);border-radius:8px;padding:12px 14px;margin-bottom:14px;}
.contact-item .lbl{color:var(--color-muted);font-size:11px;}
.contact-item .val{color:#2f6fb0;font-weight:600;margin-top:2px;}

.address-row{font-size:12px;color:#374151;}

.balances{display:grid;grid-template-columns:repeat(4,1fr);gap:16px;padding-bottom:14px;border-bottom:1px solid var(--color-line);margin-bottom:14px;}
.bal-label{font-size:11px;color:var(--color-muted);margin-bottom:4px;}
.bal-value{font-size:16px;font-weight:700;}
.bal-value.accent{color:var(--color-red);}

.detail-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:14px 20px;}

.account-details-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:0 24px;}
.ad-col{display:flex;flex-direction:column;gap:10px;}
.ad-row .field-label{margin-bottom:1px;}

.balances-table{width:100%;border-collapse:collapse;font-size:12.5px;}
.balances-table thead th{background:#fafbfc;text-align:left;color:var(--color-muted);font-weight:600;font-size:11px;padding:9px 12px;border-top:1px solid var(--color-line);border-bottom:1px solid var(--color-line);}
.balances-table td{padding:10px 12px;border-bottom:1px solid var(--color-line);font-weight:600;}
.balances-table td.accent{color:var(--color-red);}

/* Notes table */
.tabs-panel{background:var(--color-panel);border:1px solid var(--color-line);border-radius:10px;padding:16px 18px;}
.tabs-list{display:flex;gap:22px;margin-bottom:12px;flex-wrap:wrap;}
.tab-link{font-size:13px;font-weight:600;color:var(--color-muted);padding-bottom:8px;cursor:pointer;border-bottom:2px solid transparent;}
.tab-link.active{color:var(--color-green);border-bottom-color:var(--color-green);}
.table-wrap{overflow-x:auto;}
table.note-table{width:100%;border-collapse:collapse;font-size:12.5px;min-width:520px;}
table.note-table thead th{background:#fafbfc;text-align:left;color:var(--color-muted);font-weight:600;font-size:11px;padding:10px 12px;border-top:1px solid var(--color-line);border-bottom:1px solid var(--color-line);}
table.note-table td{padding:10px 12px;border-bottom:1px solid var(--color-line);vertical-align:top;}
.badge-oc{background:#eef1f4;color:#374151;padding:2px 8px;border-radius:10px;font-size:11px;font-weight:600;}

.new-note-form{display:grid;grid-template-columns:1fr 1fr 2fr auto;gap:8px;margin-top:14px;}
.new-note-form input{border:1px solid var(--color-line);border-radius:6px;padding:8px 10px;font-size:12.5px;}
.new-note-form .note-input{grid-column:span 1;}
.add-note-btn{background:var(--color-green);color:#fff;border:none;border-radius:6px;padding:8px 14px;font-size:12.5px;font-weight:600;cursor:pointer;}
.add-note-btn:disabled{opacity:0.6;cursor:default;}

/* Payment side panel */
.pay-panel{background:var(--color-panel);border:1px solid var(--color-line);border-radius:10px;padding:16px 18px;}
.pay-title{font-weight:700;color:var(--color-maroon-dark);margin-bottom:10px;}
.pay-row{display:flex;justify-content:space-between;align-items:center;padding:8px 0;border-bottom:1px dashed var(--color-line);font-size:12.5px;}
.pay-row .lbl{color:var(--color-muted);}
.pay-row .val{font-weight:600;}
.pay-row .val.big{font-size:17px;color:var(--color-red);}
.pay-section-title{background:var(--color-green-light);color:var(--color-green);font-weight:700;text-align:center;padding:8px;border-radius:6px;font-size:12.5px;margin-top:12px;}
.pay-subtitle{text-align:center;color:var(--color-red);font-weight:600;font-size:12px;margin:10px 0;}
.last-call-btn{width:100%;background:var(--color-teal);color:#fff;border:none;padding:9px;border-radius:6px;font-weight:600;margin-top:12px;cursor:pointer;}
.action-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:6px;}
.action-btn{border:none;border-radius:8px;padding:10px 2px;font-size:10.5px;line-height:1.3;font-weight:700;color:#fff;cursor:pointer;}
.action-btn.red{background:var(--color-red);}
.action-btn.amber{background:var(--color-amber);}
.action-btn.green{background:var(--color-green);}
.action-btn.gray{background:#8a94a3;}
.action-btn.maroon{background:var(--color-maroon);}
.action-btn.teal-dark{background:#1c6e78;}

.footer-bar{background:var(--color-maroon-dark);color:#fff;font-size:11px;padding:8px 22px;}

/* ===== Responsive ===== */
@media (max-width: 900px){
  .grid{grid-template-columns:1fr;}
  .info-grid{grid-template-columns:repeat(2,1fr);}
  .contact-grid{grid-template-columns:repeat(2,1fr);}
  .detail-grid{grid-template-columns:repeat(2,1fr);}
  .balances{grid-template-columns:repeat(2,1fr);}
  .account-details-grid{grid-template-columns:repeat(2,1fr);gap:14px 24px;}
}

@media (max-width: 640px){
  .hamburger{display:block;}
  .sidebar{position:fixed;top:0;left:0;bottom:0;z-index:50;transform:translateX(-100%);transition:transform .2s ease;}
  .sidebar.open{transform:translateX(0);}
  .sidebar-backdrop{display:block;position:fixed;inset:0;background:rgba(0,0,0,.35);z-index:40;}
  .content{padding:14px;}
  .info-grid{grid-template-columns:1fr 1fr;}
  .contact-grid{grid-template-columns:1fr 1fr;}
  .detail-grid{grid-template-columns:1fr 1fr;}
  .balances{grid-template-columns:1fr 1fr;gap:12px;}
  .account-details-grid{grid-template-columns:1fr;gap:12px;}
  .new-note-form{grid-template-columns:1fr;}
  .action-grid{grid-template-columns:repeat(2,1fr);}
  .case-title{font-size:15px;}
  .case-meta{flex-direction:column;align-items:flex-start;gap:6px;}
}




/* =====================================================
   DASHBOARD
===================================================== */

.dashboard-content{
  padding:18px 22px;
}


/* Dashboard Header Button */

.dashboard-primary-btn{
  border:none;
  background:var(--color-green);
  color:#fff;
  padding:8px 14px;
  border-radius:7px;
  font-size:12px;
  font-weight:600;
  cursor:pointer;
}

.dashboard-primary-btn:hover{
  background:#256b4e;
}


/* Statistics */

.dashboard-grid{
  display:grid;
  grid-template-columns:repeat(4,1fr);
  gap:14px;
  margin-top:14px;
  margin-bottom:18px;
}

.dashboard-card{
  background:var(--color-panel);
  border:1px solid var(--color-line);
  border-radius:10px;
  padding:16px 18px;
  min-height:125px;
}

.dashboard-card-label{
  color:var(--color-muted);
  font-size:11px;
  font-weight:600;
  margin-bottom:10px;
}

.dashboard-card-value{
  color:var(--color-maroon-dark);
  font-size:25px;
  font-weight:700;
  line-height:1.2;
}

.dashboard-card-footer{
  color:var(--color-muted);
  font-size:11px;
  margin-top:10px;
}

.dashboard-danger{
  color:var(--color-red);
}

.dashboard-money{
  color:var(--color-green);
}


/* Main Dashboard */

.dashboard-main-grid{
  display:grid;
  grid-template-columns:1fr 1fr;
  gap:18px;
  align-items:start;
}


/* Quick Actions */

.quick-actions{
  display:flex;
  flex-direction:column;
  gap:8px;
}

.quick-action-btn{
  width:100%;
  display:flex;
  align-items:center;
  gap:12px;
  padding:11px 12px;
  border:1px solid var(--color-line);
  border-radius:8px;
  background:#fafbfc;
  text-align:left;
  cursor:pointer;
  transition:.15s ease;
}

.quick-action-btn:hover{
  background:var(--color-green-light);
  border-color:#cce7d9;
}

.quick-action-icon{
  width:34px;
  height:34px;
  display:flex;
  align-items:center;
  justify-content:center;
  background:var(--color-green-light);
  border-radius:7px;
  font-size:16px;
}

.quick-action-btn span:last-child{
  display:flex;
  flex-direction:column;
  gap:2px;
}

.quick-action-btn strong{
  color:var(--color-ink);
  font-size:12.5px;
}

.quick-action-btn small{
  color:var(--color-muted);
  font-size:10.5px;
}


/* Recent Tasks */

.dashboard-task-list{
  display:flex;
  flex-direction:column;
}

.dashboard-task{
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:12px;
  padding:12px 0;
  border-bottom:1px solid var(--color-line);
}

.dashboard-task:last-child{
  border-bottom:none;
}

.dashboard-task > div{
  display:flex;
  flex-direction:column;
  gap:3px;
  min-width:0;
}

.dashboard-task strong{
  font-size:12px;
  color:var(--color-ink);
}

.dashboard-task span:not(.badge-acctmark-solid):not(.badge-warn){
  font-size:11px;
  color:var(--color-muted);
}


/* Responsive */

@media (max-width:900px){

  .dashboard-grid{
    grid-template-columns:repeat(2,1fr);
  }

  .dashboard-main-grid{
    grid-template-columns:1fr;
  }

}


@media (max-width:640px){

  .dashboard-content{
    padding:14px;
  }

  .dashboard-grid{
    grid-template-columns:1fr 1fr;
    gap:10px;
  }

  .dashboard-card{
    padding:14px;
    min-height:110px;
  }

  .dashboard-card-value{
    font-size:21px;
  }

  .dashboard-main-grid{
    grid-template-columns:1fr;
  }

  .case-header{
    margin-bottom:8px;
  }

}



/* =====================================================
   ACCOUNTS
===================================================== */

.accounts-content{
  padding:18px 22px;
}

.accounts-panel{
  margin-top:14px;
}


/* Table */

.accounts-table{
  width:100%;
  border-collapse:collapse;
  font-size:12.5px;
}

.accounts-table thead th{
  background:#fafbfc;
  text-align:left;
  color:var(--color-muted);
  font-weight:600;
  font-size:11px;
  padding:10px 12px;
  border-top:1px solid var(--color-line);
  border-bottom:1px solid var(--color-line);
}

.accounts-table tbody td{
  padding:12px;
  border-bottom:1px solid var(--color-line);
  vertical-align:middle;
}

.accounts-table tbody tr:hover{
  background:#fafbfc;
}


/* Customer */

.account-custno{
  color:var(--color-maroon-dark);
  font-weight:600;
  white-space:nowrap;
}

.account-customer{
  font-weight:600;
  color:var(--color-ink);
}


/* Account Mark */

.account-mark{
  display:inline-block;
  background:var(--color-green-light);
  color:var(--color-green);
  border:1px solid #cde7da;
  padding:4px 9px;
  border-radius:6px;
  font-size:11px;
  font-weight:700;
}


/* Balance */

.account-balance{
  font-weight:700;
  color:var(--color-red);
  white-space:nowrap;
}


/* Status */

.account-status{
  display:inline-block;
  padding:4px 10px;
  border-radius:12px;
  font-size:11px;
  font-weight:700;
}

.account-status.npl{
  background:#fdeceb;
  color:var(--color-red);
}

.account-status.normal{
  background:var(--color-green-light);
  color:var(--color-green);
}


/* View Button */

.account-view-btn{
  border:1px solid var(--color-green);
  background:#fff;
  color:var(--color-green);
  padding:6px 13px;
  border-radius:6px;
  font-size:11px;
  font-weight:600;
  cursor:pointer;
}

.account-view-btn:hover{
  background:var(--color-green-light);
}


/* Responsive */

@media (max-width:900px){

  .accounts-panel{
    overflow:hidden;
  }

  .accounts-table{
    min-width:700px;
  }

}

@media (max-width:640px){

  .accounts-content{
    padding:14px;
  }

  .accounts-panel{
    padding:14px;
  }

}



/* =====================================================
   DASHBOARD SEARCH ACCOUNTS
===================================================== */

.dashboard-search-panel {
  margin-top: 16px;
  padding: 0;
  overflow: hidden;
}

.dashboard-search-panel h4 {
  display: flex;
  align-items: center;
  gap: 8px;
  width: 100%;
  margin: 0;
  padding: 12px 16px;
  border-bottom: 1px solid var(--color-line);
  color: var(--color-ink);
  font-size: 12px;
  font-weight: 700;
}

.dashboard-search-panel h4::before {
  content: "⌕";
  color: var(--color-green);
  font-size: 17px;
}


/* Search fields */

.dashboard-search-grid {
  display: grid;
  grid-template-columns: 1.2fr 1fr 1.2fr 1fr;
  gap: 12px;
  padding: 14px 16px 8px;
}

.dashboard-search-field {
  display: flex;
  flex-direction: column;
  min-width: 0;
}

.dashboard-search-field label {
  color: var(--color-ink);
  font-size: 10.5px;
  font-weight: 600;
  margin-bottom: 5px;
}

.dashboard-search-field input,
.dashboard-search-field select {
  width: 100%;
  height: 32px;
  padding: 0 9px;
  border: 1px solid #d8ddd9;
  border-radius: 4px;
  background: #fff;
  color: var(--color-ink);
  font-family: inherit;
  font-size: 11px;
  outline: none;
  transition: border-color 0.15s ease,
              box-shadow 0.15s ease;
}

.dashboard-search-field input::placeholder {
  color: #a0a6ad;
}

.dashboard-search-field input:focus,
.dashboard-search-field select:focus {
  border-color: var(--color-green);
  box-shadow: 0 0 0 2px rgba(46, 125, 91, 0.08);
}


/* Select */

.dashboard-search-field select {
  cursor: pointer;
  appearance: auto;
}


/* Buttons */

.dashboard-search-actions {
  display: flex;
  justify-content: flex-end;
  align-items: center;
  gap: 8px;
  padding: 4px 16px 14px;
}

.dashboard-clear-btn,
.dashboard-search-btn {
  height: 30px;
  padding: 0 16px;
  border-radius: 4px;
  font-family: inherit;
  font-size: 10.5px;
  font-weight: 600;
  cursor: pointer;
  transition: 0.15s ease;
}

.dashboard-clear-btn {
  background: #fff;
  border: 1px solid #d8ddd9;
  color: #5f666d;
}

.dashboard-clear-btn:hover {
  background: #f5f6f7;
  border-color: #c5cbc7;
}

.dashboard-search-btn {
  background: var(--color-green);
  border: 1px solid var(--color-green);
  color: #fff;
}

.dashboard-search-btn:hover {
  background: #246b4d;
  border-color: #246b4d;
}


/* =====================================================
   SEARCH RESULTS
===================================================== */

.dashboard-results-panel {
  margin-top: 16px;
  padding: 0;
  overflow: hidden;
}

.dashboard-results-panel h4 {
  display: inline-block;
  margin: 0;
  padding: 12px 16px;
  color: var(--color-green);
  font-size: 12px;
  font-weight: 700;
  border-bottom: 2px solid var(--color-green);
}

.dashboard-results-count {
  float: right;
  margin: 13px 16px 0 0;
  color: var(--color-muted);
  font-size: 10.5px;
}


/* Results table */

.dashboard-results-panel .table-wrap {
  clear: both;
  margin-top: 0;
}

.dashboard-results-panel table {
  margin-top: 0;
}

.dashboard-results-panel table.note-table thead th {
  background: #fafbfc;
  padding: 9px 10px;
  font-size: 10px;
  white-space: nowrap;
}

.dashboard-results-panel table.note-table td {
  padding: 9px 10px;
  font-size: 11px;
  white-space: nowrap;
}

.dashboard-results-panel table.note-table tbody tr:hover {
  background: #fafdfb;
}


/* View button */

.dashboard-view-btn {
  border: 1px solid var(--color-green);
  background: #fff;
  color: var(--color-green);
  border-radius: 4px;
  padding: 4px 10px;
  font-family: inherit;
  font-size: 10px;
  font-weight: 600;
  cursor: pointer;
}

.dashboard-view-btn:hover {
  background: var(--color-green-light);
}


/* =====================================================
   RESPONSIVE SEARCH
===================================================== */

@media (max-width: 900px) {

  .dashboard-search-grid {
    grid-template-columns: repeat(2, 1fr);
  }

}

@media (max-width: 640px) {

  .dashboard-search-grid {
    grid-template-columns: 1fr;
    gap: 10px;
  }

  .dashboard-search-actions {
    justify-content: stretch;
  }

  .dashboard-clear-btn,
  .dashboard-search-btn {
    flex: 1;
  }

  .dashboard-results-count {
    float: none;
    margin: 0 16px 10px;
  }

}
/* =====================================================
   COLLECTION NOTE TABLE
===================================================== */

.note-table {
  width: 100%;
  border-collapse: collapse;
  font-size: 12px;
  table-layout: fixed;
}

.note-table th {
  background: #eeeeee;
  color: #111;
  border: 1px solid #999;
  padding: 4px 6px;
  font-weight: 600;
}

.note-table td {
  border: 1px solid #aaa;
  padding: 0;
  height: 25px;
  background: #fff;
}


/* =====================================================
   COLUMN WIDTH
===================================================== */

.note-table th:nth-child(1) {
  width: 145px;
}

.note-table th:nth-child(2) {
  width: 180px;
}

.note-table th:nth-child(3) {
  width: 260px;
}

.note-table th:nth-child(4) {
  width: auto;
}


/* =====================================================
   ACTION CODE
===================================================== */

.action-code-group {
  display: flex;
  height: 25px;
}

.action-code-group select {
  width: 75px;
  height: 25px;
  border: none;
  border-right: 1px solid #aaa;
  background: #fff;
  font-size: 12px;
  padding: 0 3px;
  cursor: pointer;
}

.action-code-group select:last-child {
  width: 75px;
  border-right: none;
}


/* =====================================================
   TELEPHONE
===================================================== */

.telephone-group {
  display: flex;
  align-items: center;
  height: 25px;
}

.telephone-group select {
  width: 75px;
  height: 25px;
  border: none;
  border-right: 1px solid #aaa;
  background: #fff;
  font-size: 12px;
}

.telephone-group span {
  padding: 0 8px;
}

.telephone-group input {
  height: 25px;
  flex: 1;
  border: none;
  padding: 0 6px;
  font-size: 12px;
}


/* =====================================================
   COLLECTION NOTE
===================================================== */
/* =====================================================
   COLLECTION NOTE - WHITE
===================================================== */

.note-table td {
  background: #fff;
}

.note-cell {
  display: flex;
  align-items: center;
  height: 25px;
  background: #fff;
}

.note-cell span {
  flex: 1;
  padding: 0 8px;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.new-note-row td {
  background: #fff;
}

.new-note-cell {
  display: flex;
  height: 25px;
  background: #fff;
}

.new-note-cell .note-input {
  flex: 1;
  height: 25px;
  border: none;
  padding: 0 7px;
  background: #fff;
  font-size: 12px;
}

.telephone-group {
  display: flex;
  align-items: center;
  height: 25px;
  background: #fff;
}

.telephone-group input {
  height: 25px;
  flex: 1;
  border: none;
  padding: 0 6px;
  font-size: 12px;
  background: #fff;
}

/* =====================================================
   COLLECTION NOTE TABLE
===================================================== */

.note-table {
  width: 100%;
  table-layout: fixed;
}

.note-table th,
.note-table td {
  box-sizing: border-box;
}

/* DateTime */
.note-table th:nth-child(1),
.note-table td:nth-child(1) {
  width: 180px;
}

/* Action Code */
.note-table th:nth-child(2),
.note-table td:nth-child(2) {
  width: 150px;
}

/* Telephone */
.note-table th:nth-child(3),
.note-table td:nth-child(3) {
  width: 220px;
}

/* Collection Note */
.note-table th:nth-child(4),
.note-table td:nth-child(4) {
  width: auto;
}

/* จัดการ */
.note-table th:nth-child(5),
.note-table td:nth-child(5) {
  width: 110px;
}


/* =====================================================
   DATETIME INPUT
===================================================== */

.edit-date-input {
  width: 100%;
  max-width: 100%;
  min-width: 0;
  height: 38px;
  padding: 0 8px;

  box-sizing: border-box;

  border: 1px solid #d1d5db;
  border-radius: 7px;

  background: #fff;
  color: #374151;

  font-size: 13px;
  font-family: inherit;
}

.edit-date-input:focus {
  outline: none;
  border-color: #2563eb;
  box-shadow: 0 0 0 2px rgba(37, 99, 235, 0.1);
}
/* =====================================================
   NOTE EDIT ACTION BUTTONS
===================================================== */

.edit-actions {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 6px;
  min-height: 36px;
}

/* ปุ่มทั้งหมด */
.edit-note-btn,
.save-note-btn,
.cancel-note-btn {
  height: 30px;
  padding: 0 11px;
  border-radius: 5px;
  font-family: inherit;
  font-size: 11px;
  font-weight: 600;
  cursor: pointer;
  transition:
    background-color .15s ease,
    border-color .15s ease,
    color .15s ease,
    transform .1s ease;
  white-space: nowrap;
}

/* =====================================================
   EDIT
===================================================== */

.edit-note-btn {
  background: #fff;
  border: 1px solid var(--color-green);
  color: var(--color-green);
}

.edit-note-btn:hover {
  background: var(--color-green-light);
  border-color: var(--color-green);
}

.edit-note-btn:active {
  transform: scale(.97);
}


/* =====================================================
   SAVE
===================================================== */

.save-note-btn {
  background: var(--color-green);
  border: 1px solid var(--color-green);
  color: #fff;
}

.save-note-btn:hover:not(:disabled) {
  background: #256b4e;
  border-color: #256b4e;
}

.save-note-btn:active:not(:disabled) {
  transform: scale(.97);
}


/* =====================================================
   CANCEL
===================================================== */

.cancel-note-btn {
  background: #fff;
  border: 1px solid #d1d5db;
  color: #6b7280;
}

.cancel-note-btn:hover:not(:disabled) {
  background: #f4f5f7;
  border-color: #b9bec5;
  color: #374151;
}

.cancel-note-btn:active:not(:disabled) {
  transform: scale(.97);
}


/* =====================================================
   DISABLED
===================================================== */

.save-note-btn:disabled,
.cancel-note-btn:disabled {
  opacity: .55;
  cursor: not-allowed;
}


/* =====================================================
   MOBILE
===================================================== */

@media (max-width: 640px) {

  .edit-actions {
    gap: 5px;
  }

  .edit-note-btn,
  .save-note-btn,
  .cancel-note-btn {
    height: 29px;
    padding: 0 9px;
    font-size: 10.5px;
  }

}
 /* =====================================================
    COLLECTION NOTE - MODERN UI
 ===================================================== */

 /* กล่อง Collection Note */
 .tabs-panel {
   background: var(--color-panel);
   border: 1px solid var(--color-line);
   border-radius: 10px;
   padding: 0;
   overflow: hidden;
 }

 /* =====================================================
    TABS
 ===================================================== */

 .tabs-header {
   background: #fff;
   border-bottom: 1px solid var(--color-line);
 }

 .tabs-list {
   display: flex;
   align-items: center;
   gap: 24px;
   margin: 0;
   padding: 0 18px;
 }

 .tab-link {
   position: relative;
   padding: 13px 2px 11px;
   font-size: 12px;
   font-weight: 600;
   color: var(--color-muted);
   cursor: pointer;
   border-bottom: 2px solid transparent;
   transition: .15s ease;
 }

 .tab-link:hover {
   color: var(--color-green);
 }

 .tab-link.active {
   color: var(--color-green);
   border-bottom-color: var(--color-green);
 }


 /* =====================================================
    TABLE WRAPPER
 ===================================================== */

 .tabs-panel .table-wrap {
   overflow-x: auto;
   width: 100%;
 }


 /* =====================================================
    TABLE
 ===================================================== */

 .tabs-panel .note-table {
   width: 100%;
   min-width: 850px;
   border-collapse: separate;
   border-spacing: 0;
   table-layout: fixed;
   font-size: 12px;
 }

 .tabs-panel .note-table th {
   height: 36px;
   padding: 8px 10px;
   background: #f8faf9;
   color: #59616b;
   border: none;
   border-bottom: 1px solid var(--color-line);
   font-size: 10.5px;
   font-weight: 700;
   text-align: left;
   white-space: nowrap;
 }

 .tabs-panel .note-table td {
   height: 40px;
   padding: 4px 8px;
   background: #fff;
   color: var(--color-ink);
   border: none;
   border-bottom: 1px solid var(--color-line);
   vertical-align: middle;
 }

 .tabs-panel .note-table tbody tr:hover td {
   background: #fafdfb;
 }

 .tabs-panel .note-table tbody tr:last-child td {
   border-bottom: none;
 }


 /* =====================================================
    COLUMN WIDTH
 ===================================================== */

 .tabs-panel .note-table th:nth-child(1),
 .tabs-panel .note-table td:nth-child(1) {
   width: 180px;
 }

 .tabs-panel .note-table th:nth-child(2),
 .tabs-panel .note-table td:nth-child(2) {
   width: 150px;
 }

 .tabs-panel .note-table th:nth-child(3),
 .tabs-panel .note-table td:nth-child(3) {
   width: 220px;
 }

 .tabs-panel .note-table th:nth-child(4),
 .tabs-panel .note-table td:nth-child(4) {
   width: auto;
 }

 .tabs-panel .note-table th:nth-child(5),
 .tabs-panel .note-table td:nth-child(5) {
   width: 125px;
   text-align: center;
 }


 /* =====================================================
    DATETIME
 ===================================================== */

 .edit-date-input {
   width: 100%;
   height: 30px;
   padding: 0 8px;

   border: 1px solid #d7ddd9;
   border-radius: 5px;

   background: #fff;
   color: #374151;

   font-family: inherit;
   font-size: 11px;

   outline: none;
   transition: .15s ease;
 }

 .edit-date-input:hover {
   border-color: #b8c8bf;
 }

 .edit-date-input:focus {
   border-color: var(--color-green);
   box-shadow: 0 0 0 2px rgba(46, 125, 91, .08);
 }

 .editable-cell {
   display: inline-flex;
   align-items: center;
   min-height: 30px;
   padding: 0 7px;

   color: #4b5563;
   font-size: 11px;

   border-radius: 5px;
   cursor: pointer;

   transition: .15s ease;
 }

 .editable-cell:hover {
   background: var(--color-green-light);
   color: var(--color-green);
 }


 /* =====================================================
    ACTION CODE
 ===================================================== */

 .action-code-group {
   display: flex;
   align-items: center;
   width: 100%;
   height: 30px;

   border: 1px solid #d7ddd9;
   border-radius: 5px;
   overflow: hidden;

   background: #fff;
 }

 .action-code-group select {
   flex: 1;
   width: 50%;
   height: 30px;

   padding: 0 6px;

   border: none;
   outline: none;

   background: #fff;
   color: #374151;

   font-family: inherit;
   font-size: 11px;

   cursor: pointer;
 }

 .action-code-group select:first-child {
   border-right: 1px solid #e2e5e3;
 }

 .action-code-group select:focus {
   background: #f8fcfa;
 }


 /* =====================================================
    TELEPHONE
 ===================================================== */

 .telephone-group {
   display: flex;
   align-items: center;

   width: 100%;
   height: 30px;

   border: 1px solid #d7ddd9;
   border-radius: 5px;
   overflow: hidden;

   background: #fff;
 }

 .telephone-group select {
   width: 78px;
   min-width: 78px;
   height: 30px;

   padding: 0 5px;

   border: none;
   border-right: 1px solid #e2e5e3;

   outline: none;

   background: #fff;
   color: #4b5563;

   font-family: inherit;
   font-size: 10.5px;

   cursor: pointer;
 }

 .telephone-group input {
   flex: 1;
   min-width: 0;
   height: 30px;

   padding: 0 8px;

   border: none;
   outline: none;

   background: #fff;
   color: var(--color-ink);

   font-family: inherit;
   font-size: 11px;
 }

 .telephone-group:focus-within {
   border-color: var(--color-green);
   box-shadow: 0 0 0 2px rgba(46, 125, 91, .08);
 }


 /* =====================================================
    NOTE INPUT
 ===================================================== */

 .note-edit-input,
 .note-view-input,
 .new-note-cell .note-input,
 .tabs-panel .note-table td > .note-input {
   width: 100%;
   height: 30px;

   padding: 0 9px;

   border-radius: 5px;

   font-family: inherit;
   font-size: 11.5px;

   outline: none;
   box-sizing: border-box;
 }

 .note-edit-input {
   border: 1px solid #d7ddd9;
   background: #fff;
   color: var(--color-ink);
 }

 .note-edit-input:focus {
   border-color: var(--color-green);
   box-shadow: 0 0 0 2px rgba(46, 125, 91, .08);
 }

 .note-view-input {
   border: 1px solid transparent;
   background: transparent;
   color: #374151;
   cursor: text;
 }

 .note-view-input:hover {
   border-color: #e0e5e2;
   background: #fafcfb;
 }

 .new-note-cell .note-input,
 .tabs-panel .note-table td > .note-input {
   border: 1px solid #d7ddd9;
   background: #fff;
 }

 .new-note-cell .note-input:focus,
 .tabs-panel .note-table td > .note-input:focus {
   border-color: var(--color-green);
   box-shadow: 0 0 0 2px rgba(46, 125, 91, .08);
 }

 input::placeholder {
   color: #a0a6ad;
 }


 /* =====================================================
    EDIT ACTIONS
 ===================================================== */

/* =====================================================
   COLLECTION NOTE - ACTION BUTTONS
===================================================== */

.edit-actions {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 5px;
  white-space: nowrap;
}


/* =====================================================
   EDIT BUTTON
===================================================== */

.edit-note-btn {
  height: 29px;
  min-width: 48px;
  padding: 0 10px;

  border: 1px solid var(--color-green);
  border-radius: 5px;

  background: #fff;
  color: var(--color-green);

  font-family: inherit;
  font-size: 10.5px;
  font-weight: 600;

  cursor: pointer;
  transition: .15s ease;
}

.edit-note-btn:hover {
  background: var(--color-green-light);
}

.edit-note-btn:active {
  transform: scale(.97);
}


/* =====================================================
   DELETE BUTTON
===================================================== */

.delete-note-btn {
  height: 29px;
  min-width: 42px;
  padding: 0 9px;

  border: 1px solid var(--color-red);
  border-radius: 5px;

  background: #fff;
  color: var(--color-red);

  font-family: inherit;
  font-size: 10.5px;
  font-weight: 600;

  cursor: pointer;
  transition: .15s ease;
}

.delete-note-btn:hover {
  background: #fdeceb;
  border-color: var(--color-red);
  color: #a93226;
}

.delete-note-btn:active {
  transform: scale(.97);
}


/* =====================================================
   SAVE BUTTON
===================================================== */

.save-note-btn {
  height: 29px;
  min-width: 52px;
  padding: 0 10px;

  border: 1px solid var(--color-green);
  border-radius: 5px;

  background: var(--color-green);
  color: #fff;

  font-family: inherit;
  font-size: 10.5px;
  font-weight: 600;

  cursor: pointer;
  transition: .15s ease;
}

.save-note-btn:hover:not(:disabled) {
  background: #256b4e;
  border-color: #256b4e;
}

.save-note-btn:active:not(:disabled) {
  transform: scale(.97);
}


/* =====================================================
   CANCEL BUTTON
===================================================== */

.cancel-note-btn {
  height: 29px;
  min-width: 52px;
  padding: 0 10px;

  border: 1px solid #d1d5db;
  border-radius: 5px;

  background: #fff;
  color: #6b7280;

  font-family: inherit;
  font-size: 10.5px;
  font-weight: 600;

  cursor: pointer;
  transition: .15s ease;
}

.cancel-note-btn:hover:not(:disabled) {
  background: #f4f5f7;
  border-color: #b9bec5;
  color: #374151;
}

.cancel-note-btn:active:not(:disabled) {
  transform: scale(.97);
}


/* =====================================================
   DISABLED
===================================================== */

.save-note-btn:disabled,
.cancel-note-btn:disabled,
.delete-note-btn:disabled {
  opacity: .5;
  cursor: not-allowed;
}


/* =====================================================
   ACTION COLUMN
===================================================== */

.note-table th:nth-child(5),
.note-table td:nth-child(5) {
  width: 125px;
  text-align: center;
}


/* =====================================================
   MOBILE
===================================================== */

@media (max-width: 640px) {

  .edit-actions {
    gap: 4px;
  }

  .edit-note-btn,
  .delete-note-btn,
  .save-note-btn,
  .cancel-note-btn {
    height: 28px;
    padding: 0 8px;
    font-size: 10px;
  }

}
.note-actions,
.edit-actions {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 6px;
  white-space: nowrap;
}

.delete-note-btn {
  border: 1px solid #dc2626;
  background: #fff1f2;
  color: #dc2626;
  padding: 6px 12px;
  border-radius: 7px;
  font-size: 13px;
  font-weight: 600;
  cursor: pointer;
  transition: 0.15s ease;
}

.delete-note-btn:hover {
  background: #dc2626;
  color: white;
}

.delete-note-btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}
/* =====================================================
   LOGOUT BUTTON
===================================================== */

.logout-item {
  width: 100%;
  display: flex;
  align-items: center;
  gap: 12px;

  margin-top: 8px;
  padding: 11px 14px;

  border: none;
  border-radius: 10px;

  background: transparent;

  color: #dc2626;
  font-family: inherit;
  font-size: 14px;
  font-weight: 600;

  text-align: left;
  cursor: pointer;

  transition:
    background 0.2s ease,
    color 0.2s ease,
    transform 0.15s ease;
}

.logout-item:hover {
  background: #fef2f2;
  color: #b91c1c;
}

.logout-item:active {
  transform: scale(0.98);
}

.logout-icon {
  width: 22px;
  height: 22px;

  display: flex;
  align-items: center;
  justify-content: center;

  font-size: 19px;
  font-weight: 700;
}

/* =====================================================
   PAYMENT PANEL
===================================================== */

.pay-panel {
  background: #ffffff;
  border: 1px solid #e5e7eb;
  border-radius: 14px;
  padding: 22px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04);
}


/* =====================================================
   HEADER
===================================================== */

.pay-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding-bottom: 18px;
  margin-bottom: 18px;
  border-bottom: 1px solid #edf0f2;
}

.pay-title {
  font-size: 18px;
  font-weight: 700;
  color: #1f2937;
}

.pay-subtitle {
  margin-top: 4px;
  font-size: 12px;
  color: #9ca3af;
}

.pay-account-badge {
  padding: 6px 10px;
  border-radius: 7px;
  background: #f3f4f6;
  color: #374151;
  font-size: 12px;
  font-weight: 600;
}


/* =====================================================
   SECTION
===================================================== */

.pay-section {
  margin-bottom: 22px;
}

.pay-section-title {
  margin-bottom: 14px;
  font-size: 13px;
  font-weight: 700;
  color: #374151;
}

/* =====================================================
   FIX PAYMENT PANEL OVERFLOW
===================================================== */

.pay-panel {
  box-sizing: border-box;
  width: 100%;
  max-width: 100%;
}

.pay-panel *,
.pay-panel *::before,
.pay-panel *::after {
  box-sizing: border-box;
}
/* =====================================================
   FORM GRID
===================================================== */

.pay-form-grid {
  display: grid;
  grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
  gap: 15px;
  width: 100%;
}
.pay-field {
  display: flex;
  flex-direction: column;
  gap: 6px;
}

.pay-field label {
  font-size: 12px;
  font-weight: 600;
  color: #6b7280;
}

.pay-field input,
.pay-field select {
  width: 100%;
  height: 40px;
  padding: 0 11px;

  border: 1px solid #dfe3e8;
  border-radius: 7px;

  background: #fff;
  color: #1f2937;

  font-size: 13px;
  outline: none;

  transition: 0.15s ease;
}

.pay-field input:focus,
.pay-field select:focus {
  border-color: #111827;
  box-shadow: 0 0 0 3px rgba(17, 24, 39, 0.06);
}


/* =====================================================
   MONEY
===================================================== */

.money-input {
  display: flex;
  align-items: center;

  height: 40px;

  border: 1px solid #dfe3e8;
  border-radius: 7px;

  overflow: hidden;
}

.money-input span {
  padding-left: 11px;

  color: #6b7280;
  font-size: 13px;
}

.money-input input {
  border: none !important;
  box-shadow: none !important;
}


/* =====================================================
   CONTACT ACTIONS
===================================================== */

.action-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 9px;
}

.action-btn {
  min-height: 64px;

  border: 1px solid transparent;
  border-radius: 9px;

  display: flex;
  align-items: center;
  justify-content: center;
  gap: 7px;

  color: white;
  font-size: 12px;
  font-weight: 600;

  cursor: pointer;

  transition:
    transform 0.15s ease,
    box-shadow 0.15s ease,
    opacity 0.15s ease;
}

.action-btn:hover {
  transform: translateY(-1px);
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.12);
}

.action-btn:active {
  transform: scale(0.98);
}

.action-icon {
  font-size: 17px;
}


/* COLORS */

.action-btn.maroon {
  background: #7f1d1d;
}

.action-btn.red {
  background: #dc2626;
}

.action-btn.teal-dark {
  background: #0f766e;
}

.action-btn.amber {
  background: #d97706;
}


/* SELECTED */

.action-btn.selected {
  outline: 3px solid rgba(17, 24, 39, 0.15);
  transform: translateY(-1px);
  box-shadow:
    0 0 0 2px #ffffff,
    0 0 0 4px #111827;
}


/* =====================================================
   LAST PHONE
===================================================== */

.last-phone-box {
  margin-top: 5px;
  margin-bottom: 16px;

  padding: 13px 14px;

  border: 1px solid #e5e7eb;
  border-radius: 9px;

  background: #f9fafb;
}

.last-phone-label {
  font-size: 11px;
  color: #9ca3af;
  margin-bottom: 4px;
}

.last-phone-number {
  font-size: 14px;
  font-weight: 600;
  color: #374151;
}


/* =====================================================
   SAVE
===================================================== */

.save-payment-btn {
  width: 100%;
  height: 44px;

  border: none;
  border-radius: 8px;

  background: #111827;
  color: white;

  font-size: 13px;
  font-weight: 700;

  cursor: pointer;

  transition: 0.15s ease;
}

.save-payment-btn:hover {
  background: #000000;
  transform: translateY(-1px);
}

.save-payment-btn:disabled {
  opacity: 0.6;
  cursor: not-allowed;
  transform: none;
}

.save-spinner {
  display: inline-block;
  margin-right: 5px;

  animation: save-spin 0.8s linear infinite;
}

@keyframes save-spin {
  from {
    transform: rotate(0deg);
  }

  to {
    transform: rotate(360deg);
  }
}


/* =====================================================
   EMPTY
===================================================== */

.pay-empty {
  padding: 40px 20px;
  text-align: center;

  color: #9ca3af;
  font-size: 13px;
}


/* =====================================================
   RESPONSIVE
===================================================== */

@media (max-width: 700px) {

  .pay-form-grid {
    grid-template-columns: 1fr;
  }

  .action-grid {
    grid-template-columns: 1fr 1fr;
  }

}.back-btn {
  display: inline-flex;
  align-items: center;
  gap: 8px;

  height: 36px;
  padding: 0 13px;

  margin-bottom: 18px;

  border: 1px solid #d1d5db;
  border-radius: 7px;

  background: #fff;
  color: #4b5563;

  font-size: 12px;
  font-weight: 600;

  cursor: pointer;

  transition: 0.15s ease;
}

.back-btn:hover {
  background: #111827;
  border-color: #111827;
  color: #fff;
}

.back-icon {
  font-size: 16px;
  transition: transform 0.15s ease;
}

.back-btn:hover .back-icon {
  transform: translateX(-3px);
}.last-phone-box {
  margin-top: 20px;
  padding: 12px 14px;
  background: #f8fafc;
  border: 1px solid #e2e8f0;
  border-radius: 10px;
}

.last-phone-label {
  font-size: 13px;
  font-weight: 600;
  color: #64748b;
  margin-bottom: 6px;
}

.last-phone-input-wrap {
  display: flex;
  align-items: center;
  gap: 6px;
}

.last-phone-icon {
  font-size: 17px;
  width: 28px;
  text-align: center;
}

.last-phone-input {
  flex: 1;
  width: 100%;
  height: 32px;          /* ลดจาก 40px */
  padding: 0 8px;
  border: 1px solid #cbd5e1;
  border-radius: 6px;
  background: #fff;
  font-size: 14px;
  color: #1e293b;
  outline: none;
  box-sizing: border-box;
}

.last-phone-input:focus {
  border-color: #2563eb;
  box-shadow: 0 0 0 2px rgba(37, 99, 235, 0.1);
}
/* =====================================================
   SIDEBAR SCROLL AREA
===================================================== */

.sidebar-scroll {
  flex: 1;
  min-height: 0;

  overflow-y: auto;
  overflow-x: hidden;

  padding: 10px 14px 16px;
}


/* ซ่อน scrollbar แต่ยัง scroll ได้ */
.sidebar-scroll::-webkit-scrollbar {
  width: 5px;
}

.sidebar-scroll::-webkit-scrollbar-thumb {
  background: #d1d5db;
  border-radius: 10px;
}


/* =====================================================
   LOGOUT
===================================================== */

.sidebar-bottom {
  flex-shrink: 0;

  padding: 12px 14px 16px;

  border-top: 1px solid #e5e7eb;

  background: #ffffff;
}

.logout-btn {
  width: 100%;
  height: 44px;

  display: flex;
  align-items: center;
  justify-content: flex-start;

  gap: 10px;

  padding: 0 14px;

  border: 1px solid #ead7d7;
  border-radius: 10px;

  background: #fff7f7;

  color: #b42318;

  font-size: 14px;
  font-weight: 600;

  cursor: pointer;

  transition: all 0.2s ease;
}

.logout-btn:hover {
  background: #feecec;
  border-color: #e5b8b8;
  color: #991b1b;
}

.logout-btn:active {
  transform: scale(0.98);
}

.logout-icon {
  width: 24px;
  height: 24px;

  display: flex;
  align-items: center;
  justify-content: center;

  font-size: 18px;
}
.footer-bar {
  width: 100%;
  min-height: 58px;

  background: #ffffff;
  border-top: 1px solid #e5e7eb;

  display: flex;
  align-items: center;

  padding: 0 28px;
  margin-top: 30px;
}

.footer-info {
  width: 100%;

  display: flex;
  align-items: center;
  gap: 24px;
}

.footer-item {
  display: flex;
  align-items: center;
  gap: 9px;
}

.footer-label {
  font-size: 11px;
  font-weight: 700;
  color: #9ca3af;
}

.footer-value {
  font-size: 13px;
  font-weight: 600;
  color: #374151;
}

.footer-phone {
  color: #166534;
  font-size: 13px;
  font-weight: 600;
  text-decoration: none;
}

.footer-phone:hover {
  text-decoration: underline;
}

.footer-divider {
  width: 1px;
  height: 24px;
  background: #e5e7eb;
}
/* =====================================================
   ACCOUNTS FILTER
   ===================================================== */

.accounts-filters {
  display: flex;
  align-items: flex-end;
  gap: 14px;
  padding: 18px;
  margin-bottom: 18px;

  background: #ffffff;
  border: 1px solid #e5e7eb;
  border-radius: 12px;

  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04);
}

.filter-group {
  display: flex;
  flex-direction: column;
  gap: 6px;
  min-width: 170px;
}

.search-group {
  flex: 1;
  min-width: 260px;
}

.filter-group label {
  font-size: 12px;
  font-weight: 600;
  color: #6b7280;
}

.filter-group input,
.filter-group select {
  height: 40px;
  padding: 0 12px;

  border: 1px solid #d1d5db;
  border-radius: 8px;

  background: #ffffff;
  color: #111827;

  font-size: 14px;
  outline: none;
}

.filter-group input::placeholder {
  color: #9ca3af;
}

.filter-group input:focus,
.filter-group select:focus {
  border-color: #2563eb;

  box-shadow:
    0 0 0 3px
    rgba(37, 99, 235, 0.1);
}

.filter-clear-btn {
  height: 40px;

  padding: 0 18px;

  border: 1px solid #d1d5db;
  border-radius: 8px;

  background: #ffffff;
  color: #374151;

  font-size: 14px;
  font-weight: 600;

  cursor: pointer;

  transition: 0.2s;
}

.filter-clear-btn:hover {
  background: #f3f4f6;
  border-color: #9ca3af;
}


/* =====================================================
   MOBILE
   ===================================================== */

@media (max-width: 900px) {

  .accounts-filters {
    flex-direction: column;
    align-items: stretch;
  }

  .filter-group,
  .search-group {
    width: 100%;
    min-width: 0;
  }

  .filter-clear-btn {
    width: 100%;
  }

}